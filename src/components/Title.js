import React from 'react';

const Title = props =>
    <div>
      <h1>{props.title}</h1>
      <p>To do:</p>
    </div>

export default Title;

/// function Title(props) {
//    return (
//        <h1>{props.title}</h1>
 //   )
//}